'use strict';

const React = require('react/addons');

const Error = React.createClass({
  render() {
    return (
      <div>
        Godammit, Pletcher, what did you do?
      </div>
    );
  }
});

module.exports = Error;
